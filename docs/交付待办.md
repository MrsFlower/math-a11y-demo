# 交付与分发 TODO（上云 / 商店 / 用户测试分发）

整理时间：2026-08-07。本文只登记"稍后要做"的交付事项，不含功能开发。

## 一、后端上云（让用户不用本机跑服务）

- [ ] **选型更新（2026-08-07）：百炼高代码应用升为首选，SAE 降为备选。**
      高代码应用 = 把 Python 项目打成 .whl 一键部署为公网 API，开发规范极轻：
      ① 入口文件 main.py ② 提供 GET /health ③ 其余路由自由（示例用 Flask，
      FastAPI 同样适用）——规则引擎/Qwen 链/静态页可整体搬迁。
      自带：API 网关 Token 鉴权（天然防刷额度）、应用观测、日志、
      环境变量管 API Key，<0.1 元/小时，停止服务即停费。
      比赛叙事加分：模型调用 + 应用部署全栈在百炼。
      SAE 优势仅剩“无平台约束”，若高代码踩坑再回退。
- [x] 高代码适配改造（2026-08-07 已完成本地部分，已验证）：顶层 main.py 入口
      （端口取 PORT/FC_SERVER_PORT 缺省 9000）；补 GET /health；requirements.txt
      用 == 固定版本。回归 34/34 + 13/13 通过。剩：装 agentscope-runtime 打 .whl
      （需阿里云 AK/SK，部署时再做）。
- [x] 代码包已打好（2026-08-07）：对齐官方 modelstudio-agent-starter 结构——
      setup.py + app/config.yml（FC_RUN_CMD 指向 /code/python/main.py）+ 端口
      8080 + 静态页随 app.static 子包上云（STATIC_DIR 双路径回退）。find_packages
      自动发现子包（首轮模拟抓出 knowledge 漏打包，已修）。云端等效模拟 5/5：
      /health、/api/health、首页、/static、红线转译全通。
      产物：dist/math_a11y_assistant_*-0.1.0-py3-none-any.whl，直接上传控制台。
      注意：sre/（Node 引擎）不上云，云端自动回退 python 引擎；API Key 在控制台
      环境变量配 LLM_API_KEY / LLM_BASE_URL。
- [ ] 部署方式选 Serverless Function，**最小实例数设 ≥1**（毫秒级热启动，
      消灭冷启动——视障用户不可接受等待）。
- [x] 创建方式已定（2026-08-07）：**控制台创建**。首次 FC+网关授权只能在
      控制台引导完成（CLI 不引导，未授权必失败）。~~顺序：先用基础对话模板跑通
      全流程 → 重新部署换成我们的 .whl~~
      **踩坑（2026-08-07 部署实测）**：模板创建的应用把启动命令固化为模板的
      `python3 /code/python/deploy_starter/main.py`，换 whl 重新部署**不会**重读
      我们的 config.yml，启动即报 no such file；启动命令不可改。**最终方案**：
      包结构完全对齐官方 starter——新增 deploy_starter/ 入口包（main.py + config.yml +
      __init__.py），setup.py 打入该包并移除顶层 main.py；端口 8080、/health、
      app.static 静态页、sys.path 回退均保留。新包
      math_a11y_assistant_41c2a084-0.1.0-py3-none-any.whl 云端等效模拟全通
      （按平台固化命令 python deploy_starter\main.py 启动：/health、/api/health、
      首页、红线转译）。后续迭代用 runtime-fc-deploy --update。
      **第二个坑（同日实测）**：FC 运行时注入 FC_SERVER_PORT=9000，入口按
      环境变量取端口被带偏，服务听 9000、平台探活 8080，实例反复重启。
      修复：deploy_starter/main.py 改为只读同目录 config.yml（与官方 starter
      一致），不读环境变量。最终包 math_a11y_assistant_d2360c5b-0.1.0-py3-none-any.whl，
      模拟时故意注入 FC_SERVER_PORT=9000 验证仍听 8080，/health、/api/health、
      红线转译全通。
- [x] 验证项（2026-08-07 实测通过）：FC 触发器鉴权为 Authorization: Bearer <token>；
      插件从扩展上下文直连云端 /api/health、/api/transcribe-symbols 全通，
      **host_permissions（https://*.fcapp.run/*）绕过 CORS，网关预检无障碍**；
      静态页经网关可访问待浏览器确认；网关 Token 鉴权已内置插件，无需再改。
- [x] 插件内置云端（2026-08-07，v0.6.0）：sidepanel.js DEFAULT_API 指向 FC 触发器地址、
      DEFAULT_TOKEN 内置鉴权，三处 fetch 全部带 Authorization 头；manifest 加 fcapp.run
      主机权限；四份用户文档同步改为「开箱即用，无需配置」；交付包已重打。
      注意：token 随插件分发，仅限内测；上架前需换成正式鉴权方案或申请公开端点。
- [x] 云端 LLM 密钥已生效（2026-08-07）：控制台配 DASHSCOPE_API_KEY，
      app/config.py 已加双名兼容（LLM_API_KEY 优先，DASHSCOPE_API_KEY 回退），
      /api/health 显示 llm.available=true。
- [x] 修复开机自检漏 token bug（2026-08-07，v0.6.1）：测试用户报「无法连接理解服务」，
      根因是侧边栏打开时的开机健康检查 fetch 没带 Authorization 头（其余三处都带了），
      云端网关 400 拒接 → 误报。已补头；另把所有报错改为携带真实原因
      （HTTP 状态码/错误消息），便于远程定位。CDP 实测开机无误报、健康检查 200。
      交付包已重打，测试用户需重装 0.6.1。
- [ ] LLM 讲解/OCR/TTS 的长请求超时确认（讲解可达 20s+，网关超时要放到 60s 以上）。

## 二、Edge 加载项商店（用户测试分发主路线）

- [ ] 注册 Edge Add-ons 开发者账号（免费，国内可直连，Win 自带 Edge）。
- [ ] 提交 **unlisted（非公开列表）**：只有拿到链接的人能装，专为小规模实测设计。
- [ ] 上架材料：
  - [x] 隐私政策（2026-08-07）：docs/隐私政策.md 已成文，上架时需放到
        可公开访问的网址（可随 Web 页一起部署到云端）。
  - [ ] 商店描述与截图（突出双模式：转译"读得出来"/理解"学得明白"）。
  - [x] 扩展图标（2026-08-07）：蓝底根号+声波，16/32/48/128 四尺寸，
        manifest icons + action.default_icon 已注册，热重载验证通过。
- [ ] 审核周期约 1~2 周，**需在用户远程测试启动前完成注册与提交**。
- [ ] Chrome Web Store 暂缓（$5、国内访问不便），Edge 跑通后再考虑。

## 三、审核等待期的替代分发方案

- [ ] **远程协助安装**：ToDesk/向日葵远程到用户电脑装解压版扩展（10 分钟/人，装一次长期可用）。
- [x] **读屏友好安装指引**（2026-08-07）：docs/插件安装指引（读屏版）.md，
      逐步按键教程（地址栏→开发人员模式→加载已解压→选 extension 文件夹）+更新/卸载/FAQ。
- [x] **一键交付包**（2026-08-07）：scripts/make_user_package.py →
      dist/公式助手_用户交付包_日期.zip（extension + 快速开始.txt + 四份文档，含测试反馈表）；
      云端地址确定后补进快速开始再重新打包。
      环境决策：测试用户非开发者，**启动服务.bat 不进包**（且包内无后端代码，bat 本就无法运行）；
      用户只走云服务零安装；本机模式仅由开发者远程协助整包部署（完整项目+环境+bat）。
      代码保护决策：**不做混淆加密**——Edge/Chrome 商店政策禁止混淆代码（会拒审）；
      已核实扩展内无密钥（API_KEY 仅为 localStorage 键名），真密钥在后端/云端环境变量。
- [x] **测试反馈表**（2026-08-07）：docs/测试反馈表.md，六节：环境信息 / 10 项核心流程打勾 /
      6 个重点体验问题（结构朗读自然度等）/ 问题描述模板 / 功能期待 / 总评；
      已同步进使用指南第 0 步（收包解压）与文末反馈入口。
- [ ] 兜底：让用户先用 Web 页（符号转译区）测核心功能，不依赖任何安装。
- [x] **已否决：打包便携浏览器发给用户**——读屏环境被连根拔起（NVDA 配置/习惯不随迁）、
      Edge 专有软件再分发有许可风险、包体大且更新困难。

## 四、可选增强（有亮点但非必须）

- [ ] 转译规则引擎移植为插件内置 JS：转译模式完全离线、零服务、零额度
      （`transcriber.py` 规则表照搬，34 条测试用例可复用为 JS 版），无障碍叙事强。
- [ ] BYO Key 备选：设置页允许技术用户填自己的百炼 Key 直连，仅作备选不做主推。

## 五、依赖关系

```
注册 Edge 账号 → 隐私政策页 + 图标 → 提交 unlisted 审核（1~2 周）
                                      ↑ 并行：SAE 部署（审核通过时后端必须已在云端，
                                        否则用户装了插件却连不上服务）
审核等待期：远程协助 + 安装指引支撑北辰等用户实测
```

## 六、Web 展示 demo 更新（static/index.html）

功能面已是最新（符号转译 + 双风格已有），缺的是「展示面」：

- [ ] 顶部加产品介绍区：一段定位文案 + 双模式理念（读得出来/学得明白）+ 适用人群，
      评审第一眼看到价值主张。
- [ ] 加「一键体验」样例区：预置红线用例 ∫f(x)e^(−iωx)dx 等 3~4 条，点按钮直接填进
      转译输入框并运行，演示零门槛。
- [ ] 加「浏览器插件」介绍区：说明产品主前台是插件、Web 页是免安装体验入口，
      链到 docs/产品介绍与使用指南.md 与安装步骤。
- [ ] 页脚补版本号与更新日期。

## 七、百炼平台发布（调研结论，2026-08-07）

结论：**可用作演示渠道，不能替代我们的正式分发路线。**

- 百炼「智能体应用（Agent 1.0）」支持发布为：魔笔网页分享、钉钉机器人、微信公众号、
  可复用组件、音视频实时互动；Agent 2.0 仅支持 API 调用。
- 硬限制：魔笔分享链接**访问者必须有阿里云账号**，视障测试用户不现实；
  且网页 UI 是百炼通用对话界面，没有我们的双模式/双风格/历史/复制/焦点管理。
- 我们的核心链路（本地规则转译 + 自研 UI）跑在百炼之外，发布上去的只能是
  「理解模式 LLM 讲解」的纯对话 demo。
- [ ] 可选：建一个 Agent 1.0（挂理解模式提示词），魔笔分享链接作为比赛评审的
      「30 秒在线体验」入口，与 Web 页/插件并列展示。成本：几十分钟配置。
- 正式用户测试仍按第二/三节：Edge unlisted + SAE 后端。
